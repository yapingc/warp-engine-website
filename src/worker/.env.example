# Supabase 服务地址（与 Web 端相同）
SUPABASE_URL=http://apps-sl.danlu.netease.com:39943

# Worker Key — 平台签发的长期 JWT，无需登录即可访问当前 schema 所有表
WORKER_KEY=

# App schema 名（与 Web 端 VITE_APP_ID 对应）
APP_SCHEMA=<%= appSchema %>
